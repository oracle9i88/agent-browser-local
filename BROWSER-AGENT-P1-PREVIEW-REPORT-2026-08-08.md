# Agent Browser Local：P1 Preview 实施记录

日期：2026-08-08

版本：0.3.0

初始范围：macOS 本地应用化与安全启动。随后按用户授权追加真实账号投稿页的只读验收；不上传、不填写、不提交、不发布。

## 已完成

- 新增 macOS `.app` 离线构建脚本，复用项目已有 Electron 运行时，不下载新依赖。
- 应用使用名称 `Agent Browser Local`、Bundle ID `com.evanguo.agent-browser-local` 和独立图标。
- 运行包只包含 `src/` 与运行时依赖 `ws`；最终 `.app` 约 275MB。
- 删除 Electron 模板的摄像头、麦克风、蓝牙与 `NSAllowsArbitraryLoads` 权限声明。
- 生成后执行 ad-hoc 深度签名并使用 `codesign --verify --deep --strict` 验证。
- 增加单实例锁；重复启动只唤醒已有窗口，不创建第二个 daemon。
- daemon 端口冲突或其他启动故障会显示本地错误窗口，不再静默退出。
- UI 增加页面故障红色状态，说明登录资料仍然保留。
- UI 显示 daemon 启动状态和当前版本；页面故障时提供仅由用户点击的“恢复页面”按钮。
- 打包验收使用临时 Profile、随机回环端口、隐藏窗口和 `about:blank`；四个 principal 均存在，第二实例正确退出，验收后无残留进程。
- 每次打包生成 `release-manifest.json`，记录 ZIP 大小与 SHA-256；打包验收会先重新计算并核对清单。
- 新增 `npm run doctor` 只读体检：不读取 token 内容、不启动浏览器，检查配置/token 权限、Profile、daemon 安全绑定和打包产物哈希。

## 产物

- `agent-chrome-lite/dist/Agent Browser Local.app`
- `agent-chrome-lite/dist/Agent Browser Local-0.3.0-mac.zip`
- `agent-chrome-lite/dist/release-manifest.json`

以上 `dist/` 产物不纳入 Git，可由 `npm run package:mac` 重建。当前为本机自用的 ad-hoc 签名版本，尚未进行 Apple Developer ID 签名与公证。

## 网易云音乐边界

网易云音乐已进入代码中的 `PLATFORM_BACKLOG`。由于当前有另一位操作者正在真实提交，本次未打开、刷新、登录或探测网易云页面，也未把未经核验的路径写入 contribution allowlist。对方完成并明确交接后，才执行官方入口核验、Snapshot 验收与最终人工发布门配置。

## 真实投稿页验收

- 小红书：通过。两次进入 `https://creator.xiaohongshu.com/publish/publish` 后，Snapshot ID/ref 均重新生成，同时稳定找回原生文件输入、“上传视频”和“上传图文”；未使用旧 ref、固定选择器或坐标。
- 快手：通过。根据官方登录回调将 allowlist 从管理页收窄为 `https://cp.kuaishou.com/article/publish/video`。首次 Snapshot 暴露数据分析侧栏后立即停止，修复并重启；两次复验均只保留上传视频、上传图文、上传全景视频等贡献控件，数据/作品/粉丝分析导航不再返回。
- 喜马拉雅：通过上传入口层复验。官方入口自然重定向到 `https://studio.ximalaya.com/upload`，登录 Profile 保留；两次重新进入后的 Snapshot ID/ref 均更新并重新找回“上传”语义入口，没有作品、数据、粉丝、收入等后台导航。当前页面仍是上传入口，未选择真实文件，因此文件选择后的标题、专辑、简介、封面等二阶段表单尚未验收。
- 抖音：通过入口层验收。首次官方投稿 URL 出现手机号与验证码登录层，Snapshot 在返回控件前正确冻结并 handoff；用户本人登录后进入上传页。第一次登录后 Snapshot 找到原生文件输入和上传入口，但同时暴露“通知、活动管理、变现中心、创作中心”等后台导航，并把上传规则说明误判为上传控件，因此当场判为未通过。修复隐私过滤、动作型上传提示判别和 SPA/最终 URL 闸门对账后，保留 Profile 重启并重新进入官方上传页，旧闸门自动清除；第二次 Snapshot 使用全新 ID/ref，仍找回文件输入和上传入口，后台导航均消失，规则说明仅保留为提示。未上传文件、填写内容或发布。
- 微信视频号：通过入口层验收。用户本人扫码登录后进入官方创建页；微信登录完成时未可靠触发 SPA 导航事件，旧 `outside_contribution_scope` 闸门一度残留，现改为每次读取状态时按最终 URL 对账，且只自动清除 outside-scope 闸门，不清登录/验证码闸门。首次 Snapshot 将“选择视频标注”误判为上传入口，修复动作词尾判别后消失；真正可点击上传区由 AX 暴露为 button，现仅在名称明确为上传动作或同时包含上传时长/大小/格式约束时提升为 `role=upload`。两次重新进入后的 Snapshot ID/ref 均更新，稳定找回上传区、短标题、草稿、手机预览和“发表”，没有后台导航或账号数据。未上传、填写或发表。
- 微信公众号：人工登录成功。直接构造编辑器 URL 时遗漏后台会话 `token`，触发重新登录；改为由用户从后台自然进入。投稿弹窗路由随后在真实页面生效：带完整会话上下文的官方编辑器已接回主受控页面。第一次真实 Snapshot 能识别封面、作者、摘要、预览、保存草稿和“发表”，但漏掉标题与富文本正文。最终修复通过 AX 父链将无 DOM ID 的语义占位提示映射到最近可编辑节点；真实 Snapshot 已稳定返回 `请在这里输入标题` 与 `从这里开始写正文` 两个独立 textbox，空值均为真正空字符串。重新进入编辑器后 Snapshot ID/ref 全部更新，仍按语义重新找回同一批字段，可重新发现性通过。未在真实公众号空稿填写测试文本：安全审查因平台可能自动保存而拒绝该动作；本地金样已覆盖多段落、空行和链接读回，真实账号侧保真仍列为未验。
- 小宇宙：复验时发现严重边界缺口，旧 registry 把整个站点配置为贡献面，导致节目选择根页的一次 Snapshot 返回了账号、节目和订阅元数据。发现后立即冻结全部自动化，没有点击节目、创建单集、上传或提交。修复后小宇宙只允许 `/podcast/:podcastId` 这种“用户已手动选定具体节目”的单层工作区 URL，明确排除根列表以及 `create`/`new`；节目根页在调用 CDP/读取无障碍树之前即返回空 Snapshot，节目实体链接也在 Snapshot 层二次剔除。旧本地配置在加载时会原子收紧，不新增已移除的平台，也不轮换 Codex、Claude、NovaGe、NovaDe token。自动测试覆盖根列表拒绝、具体节目允许、数据/互动子页拒绝、旧配置升级和防御性空 Snapshot；真实页面须在新版安装包启动后由用户手动选择节目再复验。
- 本轮没有上传文件、填写正文或点击最终提交/发布；没有打开 Suno/MIDI 或网易云音乐。

此前发现并修复的两条线上缺口均已进入代码和自动化测试：同 URL 登录层的 daemon handoff，以及快手后台侧栏的 Snapshot 隐私过滤。

小宇宙节目列表泄露属于后续发现的第三条线上缺口，严重度高于前两条：它暴露了“贡献页面选择前”的集合数据。报告不记录现场返回的账号名、节目名、订阅数或节目 ID；修复必须同时通过 URL allowlist 与 Snapshot 防御测试，不能只靠 Agent 提示词。

微信公众号另暴露两条平台事实：编辑器 URL 必须保留由站内入口产生的完整 token/context，不能手工裁剪；编辑器可能由 `window.open` 创建独立窗口。两条均由用户实际页面和截图确认。

## 工具栏状态分叉事故

用户截图证明，页面窗口顶部只显示 HTML 初始占位“本地安全模式”，没有版本号，也没有 daemon 已要求的“我已接管”按钮。此前将该现象误判为用户尚未点击，导致用户执行了无效操作。

根因与修复：

- sandboxed Electron preload 使用了 `preload.mjs`；改为受支持的 CommonJS `preload.cjs`。
- IPC handler 原先在窗口加载完成后才注册；改为先注册 IPC，再创建窗口。
- 新增强制启动自检：先确认 preload bridge 存在，再确认工具栏渲染“daemon 启动中”，监听成功后确认显示 `v0.3.0`。任一步失败即终止启动并显示错误，禁止继续展示伪安全状态。
- 第一版自检把“daemon 已就绪”断言放在 `api.listen()` 之前，被打包验收正确拦下；随后拆为两阶段自检并重新打包，临时 Profile 的 packaged smoke 通过。

用户截图是该事故的决定性证据；在截图出现前，没有足够证据支持“用户未点击”的判断。

第二次现场判断事故：编辑器接回后 daemon 的 `handoff` 已经为空，工具栏按旧设计会主动隐藏“我已接管”。仍要求用户点击该按钮属于未先核对状态的错误。现已将接管控件改为永久显示：有交接时显示可点击“我已接管”，无交接时显示禁用的“Agent 可操作”，启动中显示禁用的“Agent 未就绪”，避免内部状态与界面动作再次混淆。

## 尚未完成

- 小宇宙新版安装包重启后的真实复验：用户需在完全不暴露给 Agent 的节目列表上手动选择目标节目；随后只在具体节目工作区核验“创建单集”、标题、Show Notes、音频/封面上传和最终人工“创建”门。当前仍保持 handoff 冻结，修复安装前禁止继续 Snapshot。
- 抖音尚未上传真实文件，因此上传后的标题、描述、封面、标签等二阶段表单未验收。
- 微信视频号尚未上传真实文件，因此视频描述、封面、合集等上传后二阶段控件未验收。
- 喜马拉雅尚未上传真实音频，因此标题、专辑、简介、封面等二阶段控件未验收。
- 微信公众号真实账号侧的富文本保真写入/读回；标题/正文语义映射与重新发现性已通过，本地金样已覆盖多段落、空行和链接，但真实空稿可能自动保存，需用户明确授权后才可写入。
- 小红书/快手均未上传正式文件，因此上传后的标题、正文、封面、标签等二阶段表单尚未验收。
- 网易云音乐仍等待正在提交作品的其他操作者明确交接。
- 未操作 Suno/MIDI。
- 未安装到 `/Applications`。
- 未进行 Developer ID 签名、公证、自动更新或开机启动。
